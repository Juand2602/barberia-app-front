name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    runs-on: windows-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: 📦 Install dependencies
        run: npm ci
      
      # ⚠️ PASO CRÍTICO: Crear .env.production antes del build
      - name: 🔐 Create production environment file
        run: |
          echo "VITE_API_URL=https://barberia-app-back.onrender.com/api" > .env.production
          echo "VITE_ENV=production" >> .env.production
          echo "✅ .env.production creado con éxito"
          type .env.production
        shell: pwsh
      
      - name: 🔨 Build frontend
        run: npm run build:vite
        env:
          NODE_ENV: production
        
      - name: 🔧 Build Electron
        run: npm run build:electron
      
      - name: 📱 Build and publish app
        run: npm run dist:win
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
      
      - name: 📋 Get version
        id: version
        run: |
          $version = node -p "require('./package.json').version"
          echo "VERSION=$version" >> $env:GITHUB_OUTPUT
        shell: pwsh
      
      - name: 🚀 Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            release/*.exe
            release/*.yml
          draft: false
          prerelease: false
          generate_release_notes: true
          body: |
            ## 🎉 Versión ${{ steps.version.outputs.VERSION }}
            
            ### 📥 Instalación
            1. Descarga el archivo `.exe`
            2. Ejecuta el instalador
            3. Sigue las instrucciones
            
            ### 🔄 Actualización automática
            Si ya tienes la aplicación instalada, se actualizará automáticamente al iniciar.
            
            ### 📝 Cambios
            Ver notas de lanzamiento automáticas abajo.
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
      
      - name: ✅ Success
        run: echo "✅ Release v${{ steps.version.outputs.VERSION }} created successfully!"